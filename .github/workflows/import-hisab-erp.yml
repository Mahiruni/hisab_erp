name: Import Hisab ERP archive

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/import-hisab-erp.yml'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: import-hisab-erp
  cancel-in-progress: false

jobs:
  import:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download and verify archive
        shell: bash
        run: |
          set -euo pipefail
          python -m pip install --quiet --disable-pip-version-check --upgrade gdown
          gdown "https://drive.google.com/uc?id=1j3VHcq-j-Kg9bOWfsqxoZys62gbtA6wo" -O /tmp/erp_hisabtech-complete.zip
          test -s /tmp/erp_hisabtech-complete.zip
          echo "cc0108484cfb5343693885d0a704ec345551d40745743575b35cf1738eaef5dc  /tmp/erp_hisabtech-complete.zip" | sha256sum --check
          unzip -tq /tmp/erp_hisabtech-complete.zip

      - name: Extract project into repository root
        shell: bash
        run: |
          set -euo pipefail
          rm -rf /tmp/hisab-import
          mkdir -p /tmp/hisab-import
          unzip -q /tmp/erp_hisabtech-complete.zip -d /tmp/hisab-import
          SOURCE=/tmp/hisab-import/erp_hisabtech
          test -f "$SOURCE/package.json"
          find . -mindepth 1 -maxdepth 1 ! -name .git -exec rm -rf {} +
          cp -a "$SOURCE"/. .

      - name: Commit extracted project
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Import complete Hisab ERP project"
          git push origin HEAD:main
