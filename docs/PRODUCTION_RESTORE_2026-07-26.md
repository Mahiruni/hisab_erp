# HisabTech production restoration

This release trigger confirms that the `hisab-erp` Vercel project must deploy the restored `MahirG/HisabERP` `main` branch with the original HisabTech identity, domains, Supabase configuration, authentication routes, and production environment variables.

It must not contain or serve the Biloo ERP rebrand. Biloo ERP is maintained separately from `MahirG/biloo_group`.
