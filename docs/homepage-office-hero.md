# Homepage office hero

The homepage hero uses an Ethiopian business professional facing a laptop that displays HisabERP. The former iMac mockup is no longer rendered on the homepage.

The generated WebP payload is stored as base64 text and decoded by the cacheable `/api/homepage-hero` endpoint. This keeps the page component small while preserving a stable same-origin image URL.
